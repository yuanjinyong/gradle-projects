/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'Yuanjy' at '15-2-7 上午1:12' with Gradle 2.2.1
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at http://gradle.org/docs/2.2.1/userguide/tutorial_java_projects.html
 */


ext {
    //gradle本地缓存目录
    gradleCacheDir = gradle.gradleUserHomeDir
    //gradlew启动文件名称
    gradlewFileName = System.getProperty('os.name').contains('Windows') ? 'gradlew.bat' : 'gradlew'
    //gradlew启动文件存放目录
    gradlewDir = new File(gradleCacheDir.parentFile, gradlewFileName).exists() ? gradleCacheDir.parentFile : rootDir
    //gradlew启动文件
    gradlewFile = new File(gradlewDir, gradlewFileName)
    //gradle全局公用脚本存放目录
    gradleScriptDir = new File(gradlewDir, 'gradle')
}

apply from: "$gradleScriptDir/global.gradle"
apply from: "$gradleScriptDir/java.gradle"
apply from: "$gradleScriptDir/maven.gradle"
apply from: "$gradleScriptDir/eclipse.gradle"

apply plugin: 'eclipse'


//subprojects.findAll{!it.childProjects}.each { project ->
configure(subprojects.findAll{!it.childProjects}) { project ->
    apply plugin: 'eclipse'
    apply plugin: 'java'
    apply plugin: 'maven'
}
